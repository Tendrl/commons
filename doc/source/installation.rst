============
Installation
============

At the command line::

    $ pip install bridge_common

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bridge_common
    $ pip install bridge_common
